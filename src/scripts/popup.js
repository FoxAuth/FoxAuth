'use strict';

var browser = browser || null;
var mdui = mdui || null;

const _M = browser.i18n.getMessage;
const _L = console.log;

let $$ = mdui.JQ;

// This page is for
// + OTP Client
// + Accounts list
// + Go to options
// + Autofill blacklist website
// + QR share
// + Manual share / import
