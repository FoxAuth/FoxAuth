{
  "name": "foxauth",
  "version": "0.1.0",
  "description": "A Firefox authenticator that can autofill TOTP codes with Firefox Android support.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean && rm -rf firefox",
    "compile": "gts clean && tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "build": "bash build.sh",
    "package-debug": "npm run build && cd firefox; zip plugin.zip *"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FoxAuth/FoxAuth.git"
  },
  "author": "FoxAuth Organization",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/FoxAuth/FoxAuth/issues"
  },
  "homepage": "https://github.com/FoxAuth/FoxAuth#readme",
  "devDependencies": {
    "gts": "^0.8.0",
    "typescript": "^2.9.2"
  },
  "dependencies": {}
}
